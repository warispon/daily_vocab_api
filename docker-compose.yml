version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: vocab_mysql
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: vocabulary_db
      MYSQL_USER: vocabuser
      MYSQL_PASSWORD: vocabpass123
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  vocabapi:
    build: ./api
    container_name: vocab_api
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: mysql+pymysql://vocabuser:vocabpass123@mysql:3306/vocabulary_db
    depends_on:
      mysql:
        condition: service_healthy
    volumes:
      - ./api:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

  n8n:
    image: n8nio/n8n
    containerner_name: n8n
    ports:
      - "5678:5678"
    depends_on:
      - vocabapi
    environment:
      - N8N_BASIC_AUTHA_ACTIVE=false
      - N8N_PORT=5678
      - N8N_HOST=localhost
    volumes:
      - n8n_data:/home/node/.n8n
      
volumes:
  mysql_data: